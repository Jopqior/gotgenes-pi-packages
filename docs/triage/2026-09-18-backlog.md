---
date: "2026-09-18"
scope: "repo"
open_issues: 93
open_prs: 17
---

# Backlog triage — 2026-09-18

Seventh triage run, three days after the 2026-09-15 run, which is the record this one is accountable to.

Three days closed ten issues — including five of the prior run's top six — and closed no pull requests.
Sixteen issues and two PRs arrived in the same window, so the tracker grew 81 → 93 and 15 → 17.

Two claims were verified live against `main` this run, and one of them changes what a scope-approved PR now contains.

## Since the last triage

Phase 15 was re-sequenced on 2026-09-18 (`5a32bb78`, `b8ed8039`): #802 moved whole to Phase 16 alongside #892, #924 and #945 were adopted as new steps, and #863 was reopened after its 2026-09-07 not-planned close.
The phase's spine is now #945 → #863 → #859 → #609 → #924 → #880 → #881 → #882, and not one step has been implemented.
Phase 22 still has Steps 20 (#904) and 22 (#913) open.
The AGENTS.md split (#937) and the shell-tripwire deny rules (#938) shipped as repo work.

| Item                         | Outcome                                                                                           | Prior rank  |
| ---------------------------- | ------------------------------------------------------------------------------------------------- | ----------- |
| #920                         | Closed; the ADR 0010 remedy shipped                                                               | 1           |
| #875                         | Closed; the dropped bash unit is salvaged                                                         | 2           |
| #861                         | Closed; the operator-promoted chain-link warning shipped                                          | 3           |
| #919                         | Closed; a custom `SYSTEM.md` keeps its own sections                                               | 4           |
| #918                         | Closed; the child's `project_context` path is directory-resolved                                  | 6           |
| #864                         | **Still open** — unchanged, and still with nothing scheduled for it                               | 5           |
| #860 + PR #908               | Issue open; the PR is mergeable but now carries the declined #922 change — see Findings           | 7           |
| #927, #932, #934, #937, #938 | Closed (hotkeys, the `SYSTEM.md` break, the agent-doc audit, the AGENTS.md split, the deny rules) | —           |
| PRs #613, #740, #922         | Still open; the closes the prior run approved have not been executed                              | 16 / 30 / — |
| #10, #11, #451, #564, #660   | Still open; same                                                                                  | —           |

Disposition of everything the 2026-09-15 run deferred or approved:

| Deferred or approved item                      | Disposition this run                                                                   |
| ---------------------------------------------- | -------------------------------------------------------------------------------------- |
| #683, #695, #791, #707, #783                   | **Operator decision: continue deferring** (offered again this run and not selected)    |
| #711, #755                                     | **Operator decision: scheduled this run**, ending their repeat deferral                |
| #859, #609, #880, #881, #892, #882, #863       | Phase 15 steps; #892 and #802 moved to Phase 16, #863 reopened and re-sequenced        |
| #904, #913                                     | Phase 22 Steps 20 and 22, unchanged                                                    |
| #804, #822, #620, #751, #886, #891, #901, #912 | Deferred by the roadmaps with recorded rationale, unchanged                            |
| #519, #846                                     | Unchanged: externally blocked and blocked by construction                              |
| #472, #698                                     | Unchanged as `adjacent`/partly served; **the update is still unsent** — 86 and 46 days |
| #10, #11, #451, #564, #660, PRs #613, #740     | Closes approved 2026-09-15, none executed; carried as pending recommendations          |
| PR #922                                        | Decline still pending its charter bullet, which is still unwritten                     |
| #831, #833, #835                               | Still absent from Phase 22's sweep dispositions — third consecutive run                |

Ranks that changed and why:

- Issue #928 with PR #929 enters at 1.
  It is a configured `deny` that never fires, verified on `main`, and it arrives with a three-file mergeable PR.
- Issue #923 enters at 2 as the surviving security item after #920 closed.
- PR #908 drops from a merge candidate to a response item: its scope approval no longer describes its contents.
- Issue #944 enters the table at 4 — a silent wrong result in a tool this repo's own retro and triage sessions read.
- Issues #711 and #755 enter Band 2 by operator decision, off the repeat-deferral gate.
- Issue #735 gains a second dependant: #931 is parked on its timeout question.

## Scope alignment

### Verdicts recorded this run

| Item                   | Package              | Verdict                  | Basis                                                                                                                                                                                                                                                            |
| ---------------------- | -------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #928 + PRs #929, #930  | pi-permission-system | aligned                  | In scope: *Hardening the gates against bypass* for #929, and *named opt-in extension seams for downstream packages* for #930's registry                                                                                                                          |
| #923, #933, #941, #945 | pi-permission-system | aligned                  | Fail-closed corrections and hardening in surfaces the package owns; #923 continues ADR 0010's remit                                                                                                                                                              |
| #924, #926             | pi-permission-system | aligned                  | Effect classification and wrapper declaration; both are deferred by Phase 15's composition, not by scope                                                                                                                                                         |
| #931                   | pi-permission-system | aligned (parked on #735) | No non-goal reaches it — the nearest, *Permissive defaults, trust profiles, or workflow presets*, excludes the permissive direction and an auto-reject is fail-closed. **Operator decision**: parked on #735, which already owns the unanswered timeout question |
| #936                   | pi-permission-system | aligned                  | The three broadcasts are the package's documented cross-extension surface (`docs/cross-extension-api.md`); it wants an answer, and got one this run                                                                                                              |
| #940, #943, #944       | pi-session-tools     | aligned                  | In scope: *Rendering a transcript more usefully for whoever reads it*, and *reaching a session the existing tools cannot reach* for #943. An `offset` bound is not a new capability, so the new-tool rule does not reach #940 — the same reading #916 got        |
| #925, #935, #939, #942 | —                    | no charter               | Repo tooling and agent documentation: a test-timeout flake, the prompt-template prune, the retro lens, and the skill-offload rows                                                                                                                                |

### The charter gap is still open

The 2026-09-15 run recorded that PR #922's decline rests on a boundary no non-goal states, and recommended writing the bullet before replying.
It is still unwritten:

```console
git log --since=2026-09-15 -L '/^## Scope and non-goals/,/^## Documentation/:packages/pi-permission-system/README.md' --oneline
```

The command returns nothing.
The reply posted to PR #908 this run says the boundary is being written down rather than citing one that does not exist, which keeps rule 1 intact — but the bullet is now blocking a second answer, not a hypothetical third host.

### Carried forward

No package's `## Scope and non-goals` section changed since 2026-09-15.
Both READMEs that changed at all (`63d3221c`, `17a3684f` for pi-permission-system, `95aeb695` for pi-subagents) changed other sections, verified with `git log -L` on the section ranges.
No carried verdict is reopened.

| Carried item                                                                                         | Re-check       | Outcome                                                         |
| ---------------------------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------- |
| #603, #604, #687, #691, #720, #680, PRs #638, #671, #675, #692                                       | None triggered | Unchanged: `aligned (parked)` on #799                           |
| PR #684                                                                                              | None triggered | Unchanged: `aligned (parked)` on #882, now Phase 15's last step |
| #686, #609, #620, #472, #698, #648, #654, #608, #762, #519, #751, #780, #779, #783, #707, #706, #797 | None triggered | Unchanged                                                       |
| #10, #11, #613, #660, #740, #451, #564                                                               | None triggered | Unchanged: dispositions approved 2026-09-15, still unexecuted   |
| #860, PR #917                                                                                        | None triggered | Unchanged: `aligned`; #908's contents changed, not its verdict  |
| PR #922                                                                                              | None triggered | Unchanged: operator decline over a charter gap                  |
| #920, #875, #861, #919, #918, #927, #932                                                             | Resolved       | Closed; verdicts discharged                                     |

### Recommended dispositions

No item was classified `out of scope` this run.
The pending dispositions are all carried from 2026-09-15 and none is applied from this template.

| Item       | Disposition                                                                                                 |
| ---------- | ----------------------------------------------------------------------------------------------------------- |
| #10, #11   | Close as not-planned; replies posted 2026-08-27                                                             |
| #451, #564 | Close as not-planned (no charter; six consecutive deferrals, no external demand)                            |
| #660       | Close as a duplicate; reply posted                                                                          |
| PR #613    | Close unmerged; the run-mode non-goal reply is posted                                                       |
| PR #740    | Close unmerged; the kick-back half remains invited as a separate PR                                         |
| PR #922    | Decline — write the non-goal bullet first; the PR #908 reply this run states the boundary is being recorded |

## The prioritized table

Bugs and security first, then everything else, with the four axes applied inside each band.
A PR disposition is a response-urgency item and does not wait for its rank.

### Band 1 — security and bugs

| Rank | Item           | Kind            | Severity                               | Why now                                                                                                                                          |
| ---- | -------------- | --------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | #928 + PR #929 | issue + PR (3p) | security (fail-open)                   | An exact-server `mcp` deny never fires for a prefix-named tool, verified on `main`, and the fix is three files, mergeable and green              |
| 2    | #923           | issue (ours)    | security                               | A secret inside `bash -c` is written verbatim under `command` and masked under `executedUnit` in the same review-log record                      |
| 3    | #860 + PR #908 | issue + PR (3p) | crash on load → **respond, not merge** | Two reporters still blocked, and the PR now carries the commit from the declined #922; reply posted asking for the split                         |
| 4    | #944           | issue (ours)    | silent wrong result                    | Transcript rendering ignores `parentId`, so abandoned branches are presented as history — in the tool `/retro` and this template are told to use |
| 5    | #933           | issue (ours)    | swallowed warning                      | The permissive-bash-fallback warning — the one that exists to flag an over-broad config — is deduped against a notify nobody received            |
| 6    | #864           | issue (3p)      | crash-like                             | The TUI goes blank until a resize; open since 2026-09-01 with nothing scheduled                                                                  |
| 7    | #762           | issue (ours)    | visible bug                            | Verified still live: `config-loader.ts:62` returns `join(homedir(), ".pi", "agent")` and `PI_CODING_AGENT_DIR` appears nowhere in the package    |
| 8    | #708           | issue (3p)      | install-blocking                       | `pi install git:` still cannot work against our own `catalog:` manifests                                                                         |
| 9    | #925           | issue (ours)    | false red on contributors              | The flaky test is in the root suite `ci.yml` runs for every PR, so it can fail a contribution for reasons in another package                     |
| 10   | #874           | issue (ours)    | cosmetic → scrollback                  | The settings overlay smears chrome into scrollback; its close target PR #757 is still conflicting                                                |
| 11   | #735           | issue (3p)      | latent hang                            | A 10-minute hardcoded block on a dead parent, and now the owner of #931's timeout question too                                                   |
| 12   | #722           | issue (ours)    | latent                                 | The drain guard behind #719 is unchanged                                                                                                         |
| 13   | #895           | issue (ours)    | release defect                         | A `scripts/`-only commit cuts a user-facing patch with nothing in the tarball                                                                    |
| 14   | #879           | issue (ours)    | masked gate                            | The rumdl cache reports clean for MD057 failures CI then catches on `main`                                                                       |
| 15   | #683           | issue (ours)    | cosmetic                               | Deferred again by operator decision; listed for band completeness                                                                                |

### Band 2 — everything else

| Rank | Item                                                                  | Kind              | Severity                     | Why now                                                                                                                               |
| ---- | --------------------------------------------------------------------- | ----------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 16   | PR #930                                                               | PR (3p)           | new public seam              | Stacks on #929; a registration API is a surface to weigh on its own, and it carries four working files at the repo root               |
| 17   | #799                                                                  | issue (ours)      | keystone                     | Decides #603, #604, #691, #720, #680 and PRs #638, #671, #675, #692; deferred again by Phase 15's ADR budget                          |
| 18   | PR #921 + #906                                                        | PR + issue (3p)   | enhancement                  | Two files, `MERGEABLE/CLEAN`, and its green is fresh — neither file it touches has changed since the run                              |
| 19   | #711 + #755                                                           | issues (3p)       | enhancement                  | **Scheduled this run** by operator decision, off the repeat-deferral gate; both are small and both reporters have waited since August |
| 20   | #936                                                                  | issue (3p)        | contract answer              | Answered this run; keep the thread open until the Inspector maintainer confirms the counting semantics                                |
| 21   | #940 + #916 + #943                                                    | issues (ours)     | friction (ours)              | The session-tools trio taxes every retro and triage session; #940 is what made the #934 model lens cost three calls                   |
| 22   | #935 + #939 + #942                                                    | issues (ours)     | agent-doc follow-through     | The #937 split's residuals: prune the templates, add the unloaded-rule lens, apply the 19 offload rows                                |
| 23   | #819                                                                  | issue (ours)      | unvalidated CI               | Workflow YAML still has no lint gate, in a repo whose fork-approval argument rests on reading those files by hand                     |
| 24   | #897                                                                  | issue (3p)        | contract gap                 | Nithin745's public carrier claim; the package's own three carriers already take it                                                    |
| 25   | #736 + PR #749                                                        | issue + PR (3p)   | enhancement that kills a bug | Still conflicting and still awaiting a #746 rebase                                                                                    |
| 26   | PR #757                                                               | PR (3p)           | needs re-check               | Conflicting and red; #874 is its close target, so the capability is still wanted                                                      |
| 27   | #926                                                                  | issue (3p)        | deferred, unanswered         | Deferred by Phase 15 with rationale; the reporter has heard nothing since 2026-09-16                                                  |
| 28   | #472 + #698                                                           | issues (3p)       | response debt                | The pi-permission-model-judge update is owed at 86 and 46 days, and was owed at the last run too                                      |
| 29   | PR #615                                                               | PR (3p)           | response debt                | Twenty-seven files, conflicting, and **62 days** with no maintainer response — the longest-waiting item in the tracker                |
| 30   | #868 + #856 + #900 + #902 + #877 + #867                               | issues (ours)     | docs + tooling               | The `authorizerChain` element, JSONC, the rumdl pin, the diagram vocabulary, the import convention, the TypeScript floor              |
| 31   | #648 + #654                                                           | issues (3p)       | adjacent                     | ADR 0011 §8 names a seam for each; #648 is buildable today                                                                            |
| 32   | #779 + #780                                                           | issues (ours)     | debt                         | The undocumented scope boundaries; #780 is keystone K2                                                                                |
| 33   | PR #917                                                               | PR (3p)           | **review, not merge**        | Now `CONFLICTING`; its policy-model change belongs behind #799, and #910 needs refiling either way                                    |
| 34   | #603, #604, #691, #720, #680, #687 + PRs #638, #671, #675, #684, #692 | issues + PRs (3p) | parked                       | The #799 and #882 dependants; holding replies posted 2026-09-15                                                                       |

Everything not listed is under Deferred, Scope alignment, or owned by a phase roadmap.

## Keystones

### K1 — #799: which channels permission policy may enter through

Dependants: #603, #604, #691, #720, #680, and PRs #638, #671, #675, #692.

Unchanged for a sixth run and deferred again — Phase 15's re-sequencing spent its ADR budget on #892 and #882.
All four dependent PRs are conflicting, and the holding replies posted last run already told their authors not to rebase.

### K2 — #780: the outbound-bridge boundary

Dependants: #658, PR #693.

Unchanged, still deferred with recorded rationale.

### K3 — #882: may a chain link dismiss an `external_directory` ask for a token that names nothing on disk?

Dependants: PR #684, the reporter concern inside #859, and the allow-capable question in #472 and #620.

Still Phase 15's last step, and the phase is now eight steps long with none implemented — so the wait on PR #684 is longer than it looked three days ago.

### K4 — the front-door parity cluster is still unsequenced

Members: #831, #833, #835.

Third consecutive run flagging this.
The 2026-09-02 operator decision folded the cluster into Phase 22; `grep` for `[#831]`, `[#833]`, `[#835]` in `packages/pi-subagents/docs/architecture/architecture.md` still returns nothing, while #876 and #942 both got explicit sweep entries in the same file.
The contributor offered to open PRs and has had no answer for 20 days.

### K5 — #928: how a proxied MCP call becomes rule targets

Dependants: PRs #929 and #930.

New this run, and unusual in that the third party wrote the keystone themselves: one issue carrying the identity walkthrough, per-naming-style candidate tables, and rule-author guidance, with the two PRs split along the decision's own seam (derivation, then registration).
The issue answers both PRs, which is why #929 is ranked on the issue's evidence rather than on its own diff.

## Findings that changed a rank

- **PR #908 now contains the change PR #922 was declined for.**
  The prior run approved #908's scope and declined #922 pending a charter bullet.
  On 2026-09-17 a second commit arrived on #908's branch:

  ```text
  9e7a95ae 2026-09-17 feat: add support for extracting target path from Oh My Pi hashline e…
  d0673193 2026-09-17 fix(pi-permission-system): support OMP prompt arrays
  ```

  `gh pr view 908 --json files` now lists `src/access-intent/tool-input-path.ts` and its test — exactly PR #922's two files.
  A scope verdict recorded against a PR is a verdict about its contents at that moment, and this is the failure mode of treating it as durable.
  The reply posted this run asks for the split; the verdict itself is unchanged.

- **#929's claim is verified on `main`, and it is a fail-open rather than a missing feature.**

  ```typescript
  if (!trimmedToolName.endsWith(`_${trimmedServerName}`)) {
  if (trimmedToolName.startsWith(`${trimmedServerName}_`)) {
  ```

  (`packages/pi-permission-system/src/access-intent/mcp-targets.ts:70,74`)

  Server derivation requires a suffix match and then explicitly skips prefix-named tools, so `mcp({ tool: "github_search_code" })` produces no `github` candidate and `mcp: {"github": "deny"}` never fires.
  That is the same class as #875, which the prior run ranked 2 — a configured deny that silently does not evaluate.

- **Twelve of the seventeen open PRs are `CONFLICTING/DIRTY`.**
  The five that are not are the five newest: #908 and #922 (`UNSTABLE`, checks pending after this run's approvals), and #921, #929, #930 (`CLEAN`).
  PR #613 and PR #917 joined the conflicting set since the prior run.
  The contributor-facing queue is now almost entirely unmergeable without contributor action, which is what makes the disposition backlog — not the review backlog — the binding constraint.

- **#921's green is fresh, which is worth stating because most are not.**
  Both files it touches, `src/authority/permission-dialog.ts` and its test, have no commits since its 2026-09-12 run.
  By contrast #908's 2026-09-14 green predates four commits to `src/exposure/tool-surface-prompt.ts` and `src/handlers/before-agent-start.ts` (#919's three fixes among them), so its old check proves nothing about merging today — the newly approved run is the one to read.

- **#925 sits in the path every contributor PR takes.**
  `ci.yml:39` runs `pnpm run test` at the root, which is the parallel run the flake is load-sensitive to.
  Recorded as measured: the failing test and the root-run mechanism are both confirmed from the issue and the workflow; I did not observe it failing a contributor's run, so its rank rests on exposure rather than an incident.

- **The fork-approval gate is per-contributor, not per-fork.**
  georgeharker's runs on `mcp-prefix-derivation` and `mcp-proxy-registration` executed without approval from their first push, while JasonLandbridge's required approval again on 2026-09-17.
  The Step 3 audit is therefore not a gate on every fork run — some execute before a maintainer ever looks.
  That is a GitHub repository setting rather than a defect, but the audit should stop being described as if it precedes all fork execution.

- **PR #930 carries four working files at the repository root.**
  `mcp-proxy-registration-PR.md`, `prefix-derivation-PR.md`, `prefix-derivation-review.patch`, and `upstream-issue.md` are in its file list.
  Asked for in the reply posted this run rather than held for review.

- **Every close the prior run approved is still open.**
  Seven items (#10, #11, #451, #564, #660, PRs #613, #740) plus the #922 decline.
  Three days is not a delay worth a finding on its own; what makes it one is that the prior run recorded these as decided, so a reader of that document would believe them done.

## CI and security audit

Re-audited in full.

| Check                                         | Result                                                                                                                                       |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Privileged triggers repo-wide                 | None. `grep -E 'pull_request_target\|workflow_run\|issue_comment' .github/workflows/*.yml` returns nothing                                   |
| `ci.yml` triggers                             | `push` to `main` and plain `pull_request`                                                                                                    |
| `release.yml` triggers                        | `workflow_dispatch` **only** — verified by reading the file, not assumed                                                                     |
| `prepare` / `publish` / `github-release` jobs | Unreachable from a pull request; the workflow has no PR trigger                                                                              |
| `label-issues.yml` (`issues: write`)          | Ours: `issues: [opened]`, `contents: read`, issue number passed via env rather than interpolated into `run:`                                 |
| Interpolation into a `run:` block             | One: `${{ github.base_ref }}` in the Fallow audit step. Accepted — a fork cannot set it to arbitrary text. `github.head_ref` is used nowhere |
| PRs touching CI, install, or `.pi/` surfaces  | Neither pending PR does. #908 and #922 touch only `packages/pi-permission-system/{src,test}`                                                 |

The `.pi/` entry is new, added on the prior run's recommendation after PR #917 edited a package skill file.

Two runs were approved after the audit passed:

| Run         | Branch                        | PR   |
| ----------- | ----------------------------- | ---- |
| 35203139893 | `fix/omp-prompt-array-compat` | #908 |
| 35202905453 | `fix/edit-tool-correction`    | #922 |

Approving #922's run is not a reversal of its decline — it gives the contributor a check result on a branch they are still pushing to, and its diff touches no privileged surface.

## Blocked on others

| Item                 | Waiting on                                                                    | Since                                       |
| -------------------- | ----------------------------------------------------------------------------- | ------------------------------------------- |
| PR #613              | Close approved 2026-09-15, not executed                                       | 2026-07-18 (62 days)                        |
| PR #615              | Rebase, plus a maintainer answer that has never come                          | 2026-07-18 (62 days)                        |
| PRs #638, #675, #692 | #799; holding replies posted, rebase discouraged                              | 2026-07-23 / 07-27 / 08-01                  |
| PR #671              | #799                                                                          | 2026-07-27 (53 days)                        |
| PR #684              | #882, now Phase 15's eighth step                                              | 2026-07-28 (52 days)                        |
| PR #693              | #780; contributor rebase                                                      | 2026-08-01 (48 days)                        |
| PR #740              | Close approved 2026-09-15, not executed                                       | 2026-08-15 (34 days)                        |
| PR #749              | Contributor rebase over the #746 breaking change                              | 2026-08-15 (34 days)                        |
| PR #757              | Us — conflicting, and #874 still wants the capability                         | 2026-08-16 (33 days)                        |
| PR #908              | Contributor — asked this run to drop `9e7a95ae`                               | 2026-09-10 (8 days)                         |
| PR #917              | Us — a review and a reply about the #910 close comment                        | 2026-09-11 (7 days)                         |
| PR #921              | Us — green, mergeable, two files                                              | 2026-09-12 (6 days)                         |
| PR #922              | Us — the charter bullet, then the decline                                     | 2026-09-14 (4 days)                         |
| PRs #929, #930       | Us — acknowledged this run; #929 is rank 1                                    | 2026-09-16 (2 days)                         |
| #735                 | Reporter to confirm on ≥26.2.0                                                | 2026-08-14 (35 days)                        |
| #660, #608           | Reporter answers to questions posted 2026-08-27                               | 2026-07-26 / 07-16                          |
| #831, #833, #835     | Us — the fold-into-Phase-22 decision was taken and never executed             | 2026-08-29 (20 days)                        |
| #472, #698           | Us — the model-judge update, owed and unsent for a second run                 | 2026-06-24 (86 days) / 2026-08-03 (46 days) |
| #519                 | Us, but externally gated on Pi SDK `UIContext` evolution                      | 2026-07-01 (110 days)                       |
| #926                 | Us — deferred with rationale the reporter has not been told                   | 2026-09-15 (3 days)                         |
| #936                 | Reporter — answered this run, awaiting confirmation                           | 2026-09-17 (1 day)                          |
| #910                 | Us — closed by its author; the defect has no tracker item if #917 is declined | 2026-09-10                                  |

## Deferred

| Item                                             | Rationale                                                                                      |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| #945, #863, #859, #609, #924, #880, #881, #882   | Phase 15 Steps 1–8; they work through the phase, not this list                                 |
| #904, #913                                       | Phase 22 Steps 20 and 22                                                                       |
| #802, #892, #822                                 | Moved to Phase 16 (the sandbox record) with recorded rationale                                 |
| #804                                             | Deferred to Phase 17 — it mirrors the `commandEffects` shape #880 creates                      |
| #926                                             | Deferred by Phase 15 for the same reason as #804: two steps deciding one config shape          |
| #620                                             | Deferred by the roadmap for a 4th consecutive sweep; #698 and #706 fold into it when scheduled |
| #797                                             | Adopted as #880's acceptance case and #882's example population; it rides Phase 15             |
| #751, #886, #891, #901, #912                     | Deferred by the phase roadmaps with recorded rationale                                         |
| #683, #695, #791, #707, #783                     | **Operator decision this run**: offered for scheduling and not selected; all stay deferred     |
| #519                                             | Externally blocked on Pi SDK `UIContext` evolution, with no in-repo lever                      |
| #846                                             | Blocked by construction; it revisits pi-nocd once #801's prompt-tail change settles            |
| #641, #676                                       | Folded into Phase 22 design input and overlapping requests respectively                        |
| #10, #11, #451, #564, #660, PRs #613, #740, #922 | Not deferred — see Scope alignment for each disposition                                        |

## Mutations performed

### Labels applied

| Issue | Applied                           | Basis                                                                                          |
| ----- | --------------------------------- | ---------------------------------------------------------------------------------------------- |
| #928  | `bug`, `pkg:pi-permission-system` | A verified fail-open in the `mcp` surface, filed outside the issue forms with no labels at all |

Every other issue opened since 2026-09-15 carries a severity label and a package label.

### CI runs approved

Runs 35203139893 (#908) and 35202905453 (#922), after the audit above passed.

### Contributor comments posted

Four, drafted with the `github-voice` skill and confirmed before posting.

| Thread  | Content                                                                                                                                                                                                                                                       |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #936    | Confirms the three broadcasts as the intended contract and names the counting edges: `ready` is not one-per-session, `ui_prompt` means the human is about to be asked rather than a request was created, and `decision` is a superset joinable on `requestId` |
| #928    | Confirms the derivation gap with the `main` code, states the review order (#929 then #930), and asks for the four root-level working files to be dropped from #930                                                                                            |
| #931    | Records the parked-on-#735 verdict, with the reason stated as one timeout question rather than two                                                                                                                                                            |
| PR #908 | Asks for `9e7a95ae` to be dropped, states the boundary is being written into the non-goals, and notes the CI approval                                                                                                                                         |

No issue or PR was closed or merged.

## Open questions recorded for the next run

- A scope verdict is a verdict about a diff, and PR #908 shows what happens when it is read as a verdict about a PR.
  The Scope alignment section should record the head SHA a PR verdict was taken against, so a later run can tell whether the thing it approved is still the thing in front of it.
- The charter bullet PR #922 needs is now blocking two answers instead of one, and it has survived two triages as a recommendation.
  A recommendation that nothing executes is indistinguishable from a deferral; this one wants an issue or a commit, not a third mention.
- The audit's description of fork approval is wrong in a way that matters. georgeharker's first push executed CI without any approval, so "approve only after the audit passes" describes a gate that does not exist for every contributor.
  Either the repository setting changes, or Step 3's wording should say what it actually controls.
- Twelve of seventeen PRs are conflicting and five of the seven approved closes are untouched, which says the constraint is execution rather than decision.
  The prior run found that a decision gate moved the disposition backlog; this run suggests the next lever is an execution step — a session that does the closes, not a template that approves them.
- #925 is the first flake ranked on exposure rather than an incident.
  If it does fail a contributor's run before the next triage, that is the evidence its rank is currently borrowing.
